import Button from '@ff/ui-kit/lib/Button';
import React from 'react';

const ResendForApproval: React.FC = () => (
  <Button variant="fill" type="primary">
    повторно отправить на согласование
  </Button>
);

export default ResendForApproval;
